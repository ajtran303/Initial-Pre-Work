// Comparison operators can be used to introduce more complex logic. These operators return a boolean value of true or false.

// The equality operator == compares two values and returns true if they are equivilent (if they are not, it returns false).

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
