// All comparison operators will return a boolean true of false value. If else statements don't need to be used for a comparison.

function isLess(a, b) {
  // Fix this code
  return (a < b);
}
  // Change these values to test
console.log(isLess(25, 15));
