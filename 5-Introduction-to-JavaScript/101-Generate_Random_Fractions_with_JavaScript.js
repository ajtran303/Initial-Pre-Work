// The Math.random() function will generat a random decimal number greater than or equal to 0 and less than 1.

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
