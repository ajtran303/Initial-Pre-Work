// Use the import syntax to write a statement that uses a function from an external file or module without having to load the whole file or module.

// This feature is a non-browser feature and uses file paths from directories.

import { capitalizeString } from "string_functions"
"use strict";
capitalizeString("hello!");
